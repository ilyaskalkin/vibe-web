# MoneyCoach MVP Testing

Simple MVP web app built with **Vite + React + TypeScript**.

## Development

Install dependencies:

```bash
npm install
```

Run the dev server:

```bash
npm run dev
```

Then open the printed local URL in your browser (by default `http://localhost:5173`).



